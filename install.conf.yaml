- defaults:
    link:
        create: true
        relink: true

- clean: ["~"]

- link:
    ~/.gitconfig:
    ~/.gitignore_global:
    ~/.git_commit_template.txt:
    ~/.shell:
    ~/.vimrc:
    ~/.zsh:
    ~/.zshrc:
    ~/.tmux.conf:
    ~/.config/starship.toml: starship.toml
