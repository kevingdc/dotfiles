- defaults:
    link:
        create: true
        relink: true

- clean: ["~"]

- link:
    ~/.eslintrc:
    ~/.gitconfig:
    ~/.gitignore_global:
    ~/.git_commit_template.txt:
    ~/.shell:
    ~/.vimrc:
    ~/.zsh:
    ~/.zshrc:

- create:
    - ~/.nvim
